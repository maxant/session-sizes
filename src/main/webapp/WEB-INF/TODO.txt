pom needs to declare that the app is a 3.1 servlet
versions of spring / JSF in applicationContext.xml, web.xml and faces-config.xml
maven provided dependency on jsp and servlet apis
once got spring 4.1, update pom to use java 1.8 rather than 1.7
